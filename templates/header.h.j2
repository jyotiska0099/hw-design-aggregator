/* Auto-generated by Hardware Design Aggregator */
/* Device: {{ device.name }} {% if device.version %}v{{ device.version }}{% endif %} */
/* CPU: {{ device.cpu_name or "Unknown" }} */

#ifndef {{ device.name | upper }}_H
#define {{ device.name | upper }}_H

#include <stdint.h>

{% for peripheral in device.peripherals %}
/* ============================================================
 * {{ peripheral.name }}{% if peripheral.description %} — {{ peripheral.description }}{% endif %}

 * Base Address: 0x{{ "%08X" | format(peripheral.base_address) }}
 * ============================================================ */
#define {{ peripheral.name }}_BASE  (0x{{ "%08X" | format(peripheral.base_address) }}UL)

{% for register in peripheral.registers %}
/* {{ register.name }}{% if register.description %} — {{ register.description }}{% endif %} */
#define {{ peripheral.name }}_{{ register.name }}_OFFSET  (0x{{ "%03X" | format(register.address_offset) }}UL)
#define {{ peripheral.name }}_{{ register.name }}  (*(volatile uint32_t *)({{ peripheral.name }}_BASE + {{ peripheral.name }}_{{ register.name }}_OFFSET))

{% for field in register.fields %}
#define {{ peripheral.name }}_{{ register.name }}_{{ field.name }}_POS  ({{ field.bit_offset }}UL)
#define {{ peripheral.name }}_{{ register.name }}_{{ field.name }}_MSK  (0x{{ field.bit_width | bit_mask(field.bit_offset) }}UL)
{% endfor %}

{% endfor %}
{% endfor %}

#endif /* {{ device.name | upper }}_H */